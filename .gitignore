# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*

# Environment / secrets
.env
.env.*
!.env.example

# Captured eval pools — paid model output, not source. A specific pool may be
# committed DELIBERATELY (git add -f) when it becomes the evidence behind a
# written finding; the directory is ignored so none is committed by accident.
eval-pools/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Test / coverage
coverage/
.vitest/
