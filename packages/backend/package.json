{
  "name": "@humanlens/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "eval": "tsc -b && node --env-file-if-exists=.env dist/eval/run-lens.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.106.0",
    "@fastify/swagger": "^9.7.0",
    "@fastify/swagger-ui": "^6.0.0",
    "@humanlens/shared": "*",
    "awilix": "^13.0.5",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.1.9"
  }
}
